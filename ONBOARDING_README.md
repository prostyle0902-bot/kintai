# 入社登録（onboarding.html）

新しくパート・アルバイトが入るときに、**1回の登録で PA勤怠・勤怠・シフト作成にまとめて反映する**ための仕組みです。

これまでは新しい人が入るたびに、3か所を手で直す必要がありました。

| 直していた場所 | 何を足していたか |
| --- | --- |
| `kintai_part.html`（PA勤怠） | PIN・氏名・所属・打刻できる場所 |
| `shift.html`（シフト作成） | 配属する現場のスタッフ一覧 |
| `shift.html` の `PART_TIME_STAFF` | 出勤簿（給与）へ転記する人 |

これを **共通スタッフ名簿** に一本化しました。
`onboarding.html` で1回登録すれば、3つのアプリが同じ名簿を読むので、コードを直す必要はありません。

```
                    ┌─────────────────────┐
                    │  入社登録            │
                    │  onboarding.html    │
                    └──────────┬──────────┘
                               │ 登録・退職・異動
                    ┌──────────▼──────────┐
                    │  共通スタッフ名簿    │
                    │  （staffシート）     │
                    └──────────┬──────────┘
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
      ┌────────────────┐ ┌─────────────┐ ┌──────────────┐
      │ PA勤怠          │ │ 勤怠         │ │ シフト作成    │
      │kintai_part.html│ │ index.html  │ │ shift.html   │
      │ パートの打刻    │ │ 社員の打刻   │ │ 現場へ配属    │
      └────────────────┘ └─────────────┘ └──────────────┘
                                              │
                                              ▼
                                   シフト管理アプリ・出勤簿へ転記
```

名簿はシフト作成の共有で使っている **同じスプレッドシート** に、`staff` という名前のシートで入ります。
新しくスプレッドシートを作る必要はありません。

---

## アプリを開く

すべて GitHub Pages で公開されています。ブックマークしてお使いください。

| アプリ | URL |
| --- | --- |
| **入社登録** | https://prostyle0902-bot.github.io/kintai/onboarding.html |
| PA勤怠（パート・アルバイト） | https://prostyle0902-bot.github.io/kintai/kintai_part.html |
| 勤怠（社員） | https://prostyle0902-bot.github.io/kintai/index.html |
| シフト作成 | https://prostyle0902-bot.github.io/kintai/shift.html |

## 準備（最初に1回だけ）

### 1. Apps Script を更新する

`shift-sync.gs` に名簿を預かる処理を足してあります。いま動いているスクリプトを差し替えてください。

1. 共有用のスプレッドシートを開く → **拡張機能 → Apps Script**
2. エディタの中身をすべて消して、この `shift-sync.gs` の内容を貼り付ける
3. `var TOKEN = '...'` が、いま使っている合言葉のままかを確認する
4. 右上の **デプロイ → デプロイを管理 → 鉛筆（編集）→ バージョン「新バージョン」→ デプロイ**

> **URLは変わりません。** 「新しいデプロイ」ではなく「デプロイを管理」から更新すれば、
> いまシフト作成アプリに設定してあるURLがそのまま使えます。

### 2. 入社登録アプリを開いて、つなぎ先を確認する

`onboarding.html` を開き、**「はじめの取り込み・設定」** タブへ。

- **ウェブアプリのURL** … 空欄のままで結構です（シフト作成と同じURLが初めから入っています）
- **合言葉（TOKEN）** … 同上
- **あなたの名前** … 誰が登録したかを名簿に残すためのものです。入れておくと後で追いやすくなります

右上のバッジが **「名簿 ○名」** になれば、つながっています。

### 3. いまの名簿を取り込む

同じタブの **「2. いまの名簿を取り込む」** を押します。
PA勤怠・勤怠アプリに直接書かれている **58名** が、PIN・所属・打刻場所・配属現場ごと共通名簿に移ります。

- すでに名簿にいる人は飛ばすので、間違って押し直しても増えません
- **PINは変わりません。** いま使っている番号がそのまま引き継がれます

続けて **「3. シフトにだけ名前がある人」** を確認してください。
シフト作成には名前があるのに勤怠アプリには登録がない方（下記5名）です。
打刻が必要な方だけチェックを入れて取り込むと、PINが自動で作られます。

| 氏名 | 現場 |
| --- | --- |
| 河野竜二 | りゅうちゃん |
| 濱田歩 | りゅうちゃん |
| 茂木多美子 | 焼きたて屋鹿島 |
| 鎌形房枝 | ゆう屋 |
| 宮内眞知子 | 神宮・亀甲堂 |

### 4. 各アプリを開き直す

PA勤怠・勤怠・シフト作成を一度読み込み直せば、名簿を読みにいきます。準備は以上です。

---

## 新しい人が入ったとき

`onboarding.html` の **「入社登録」** タブで入力して、**「この内容で登録する」** を押すだけです。

| 入力する項目 | 説明 |
| --- | --- |
| **区分** | パート・アルバイト／社員。どちらの勤怠アプリに入るかが決まります |
| **入社日** | 記録用です |
| **氏名** | 勤怠アプリの画面にこのまま出ます |
| **フリガナ** | 名簿を探すときに使えます |
| **配属する現場** | 選ぶと、その現場のシフト作成に自動で入ります。**掛け持ちは複数選べます** |
| **所属の表示** | 勤怠アプリのバッジ。現場を選ぶと自動で入ります |
| **PIN** | 空欄なら、誰とも重ならない4桁を自動で作ります |
| **出勤簿への転記** | シフトを転記したとき、この人の分を出勤簿にも送るか |
| **打刻できる場所** | 選んだ現場の場所に自動でなります。直行直帰なら「場所を問わない」 |
| **勤務できる曜日・祝日** | シフト作成の割り当てに使います |
| **勤務時間** | 現場の既定でよければそのまま。この人だけ別なら指定します |
| **出勤日数の目安・上限** | 決まっていなければ 0（指定なし）のままで結構です |

登録すると完了画面に **PIN が大きく出ます**。
**「本人に渡す案内をコピー」** を押すと、PINと所属をまとめた文面がコピーされるので、
そのままメールやLINEでお渡しください。

### 登録すると、どこに何が入るか

| アプリ | 入る内容 |
| --- | --- |
| PA勤怠 / 勤怠 | PINでログインして打刻できるようになります |
| シフト作成 | 選んだ現場すべてのスタッフ一覧に、曜日・時間・日数の条件つきで追加されます |
| 出勤簿（給与） | 「転記する」にしていれば、シフト転記のときに一緒に送られます |

> すでに開いている画面には自動では出ません。一度読み込み直してください。

---

## 退職・異動のとき

### 退職

**「名簿」** タブでその人の **「退職」** を押し、退職日を入れます。

- PA勤怠・勤怠アプリの一覧から外れ、**PINが使えなくなります**
- シフト作成では、どの現場でも「使わない」になり、割り当てられなくなります
- **名簿からは消えません。** 過去の記録はそのまま残り、必要なら「復帰」で戻せます

### 異動（配属先が変わった）

**「名簿」** タブで **「編集」** を押し、配属する現場を選び直して登録します。

- 新しい現場のスタッフ一覧に追加されます
- 前の現場では「使わない」になります（消さないので戻せます）

### 名前・PIN・条件を変えたいとき

同じく **「編集」** から直します。PINを変えたいときは4桁を直接入力してください
（他の人と重なっていれば、誰が使っているかを教えてくれます）。

---

## 勤怠とシフトで名前の表記がちがう人

これまで、勤怠アプリとシフト作成で表記が食い違っている方がいました。
共通名簿では両方を持つので、転記のときに別人になることがなくなります。
取り込みのときに自動で設定されます。

| 勤怠アプリ | シフト作成 |
| --- | --- |
| 綱代愛 | 網代愛 |
| 山﨑悠人 | 山崎悠人 |
| 小金澤登美 | 小金澤登美江 |
| ナディシャ | ナディーシャ |
| 宮川 玲奈 | レナ |
| 石毛 貴英 | 石毛貴英 |
| 土屋 芳広 | 土屋芳広 |
| 及川 晶雄 | 及川晶雄 |

新しく登録する人でシフト表だけ別の呼び方にしたい場合は、
入社登録の **「補足」→「シフト表での表記」** に入れてください。

---

## 仕組み（技術的なこと）

### 名簿の置き場所

共有用スプレッドシートの `staff` シートに、1人1行で入ります。
直接シートを見て確認することもできます（手で直すのはおすすめしません）。

| 主な列 | 内容 |
| --- | --- |
| `id` | 社内の通し番号。あとから変わりません |
| `status` | `active`（在籍）／`retired`（退職） |
| `kind` | `part`（パート・アルバイト）／`staff`（社員） |
| `pin` | 勤怠アプリの4桁PIN |
| `name` | 氏名（勤怠アプリの表示名） |
| `dept` | 勤怠アプリの所属バッジ |
| `storeIds` | シフト作成の現場ID。掛け持ちはカンマ区切り |
| `shiftName` | シフト表での表記（空なら `name` と同じ） |
| `locs` | 打刻できる場所（JSON）。空なら場所を問わない |
| `kyuyo` | 出勤簿へ転記するか |
| `days` / `holidayOk` | 勤務できる曜日・祝日の可否 |
| `startTime` 〜 `endTimeWeekend` | この人だけの勤務時間（空なら現場の既定） |
| `targetDays` / `maxDays` / `maxPerWeek` | 出勤日数の目安・上限 |

### 各アプリの読み方

- **PA勤怠 / 勤怠** … 開いたときに名簿を読み、画面内の一覧に足し引きします。
  読めたものは端末に控えるので、**次からは通信が切れていても新しい人でログインできます**。
  控えも無ければ、コードに書いてある元の一覧のまま動きます（打刻が止まることはありません）。
- **シフト作成** … 共有を読み込んだあとに名簿を取り込み、配属先の現場へ足します。
  誰かが更新していないか見にいくついでに、名簿の更新も確認します。
  **この画面で手で足した人は名簿にいないので、触りません。**

### 現場を増やしたいとき

`onboarding.html` の `SITES` に1行足してください。
`id`（現場ID）と `name` は `shift.html` の `ROSTER` と、`dept` は勤怠アプリの表記と
**まったく同じ**にしてください。ずれると別の現場・別人として登録されます。

```js
{ id:'s_atarashii', grp:'業務課', name:'新しい現場', dept:'新しい現場',
  locs:[{name:'新しい現場', lat:35.0000, lng:140.0000, radius:300}],
  start:'09:00', end:'18:00' },
```

---

## 困ったとき

| 症状 | 見るところ |
| --- | --- |
| 右上が「未設定」「読込できず」になる | 「はじめの取り込み・設定」タブのURLと合言葉。Apps Script を新バージョンでデプロイし直したか |
| 登録したのに勤怠アプリに出ない | そのアプリを読み込み直す。区分（パート／社員）が合っているか |
| 登録したのにシフト作成に出ない | 配属する現場を選んだか。シフト作成の「共有用のURL」が入っているか |
| 「PIN ○○○○ は △△ さんが使っています」 | 別の番号にするか、空欄にして自動採番にする |
| 「○○ さんはすでに登録されています」 | 名簿タブで探して「編集」から直す |
