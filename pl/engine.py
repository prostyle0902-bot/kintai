#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""freeeカード明細 → PL転記エンジン（仕様【4】）"""
import os
import pandas as pd, unicodedata, math, re
from decimal import Decimal, ROUND_FLOOR

# ---- 取引先マスタ（カード明細用）: キーワード, 税率, 分類, PL行 ----
MASTER = [
 ("タイヨー",8,"スーパー","仕入（freeeカード）"),
 ("カスミ",8,"スーパー","仕入（freeeカード）"),
 ("ｷﾞﾖｳﾑｽ-ﾊﾟ-",8,"スーパー","仕入（freeeカード）"),
 ("業務スーパー",8,"スーパー","仕入（freeeカード）"),
 ("セイミヤ",8,"スーパー","仕入（freeeカード）"),
 ("ビッグハウス",8,"スーパー","仕入（freeeカード）"),
 ("さえき",8,"スーパー","仕入（freeeカード）"),
 ("道の駅",8,"直売所","仕入（freeeカード）"),
 ("MEGAドン・キホーテ",8,"ディスカウント","仕入（freeeカード）"),
 ("ローソン",8,"コンビニ","仕入（freeeカード）"),
 ("丸や",8,"食品","仕入（freeeカード）"),
 ("ﾀﾍﾞﾁﾖｸ",8,"ネット食材","仕入（ネット食材）"),
 ("イーアスつくば",8,"食品","仕入（freeeカード）"),
 ("ウエルシア",8,"ドラッグ","消耗品費（freeeカード）"),
 ("マツモトキヨシ",8,"ドラッグ","消耗品費（freeeカード）"),
 ("クスリのアオキ",8,"ドラッグ","消耗品費（freeeカード）"),
 ("酒蔵やまなか",10,"酒","仕入（やまなか）"),
 ("ベストリカー",10,"酒","仕入（freeeカード）"),
 ("リカー＆フーズひしや",10,"酒","仕入（freeeカード）"),
 ("ミトエクセル",10,"酒","仕入（freeeカード）"),
 ("コダマインリョウ",10,"飲料","仕入（freeeカード）"),
 ("ヒーロー佐原店",10,"酒","仕入（freeeカード）"),
 ("AMAZON",10,"通販","仕入（freeeカード）"),
 ("(ﾗｸﾃﾝ",10,"楽天","仕入（freeeカード）"),
 ("MonotaRO",10,"資材","消耗品費（freeeカード）"),
 ("シモジマ",10,"包材","消耗品費（freeeカード）"),
 ("ミクリード",10,"業務用","仕入（freeeカード）"),
 ("ラクスル",10,"印刷","広告宣伝費（共通宣伝費）"),
 ("プリントパック",10,"印刷","広告宣伝費（共通宣伝費）"),
 ("カインズ",10,"ホームセンター","消耗品費（freeeカード）"),
 ("DCM",10,"ホームセンター","消耗品費（freeeカード）"),
 ("コメリ",10,"ホームセンター","消耗品費（freeeカード）"),
 ("KOMERI",10,"ホームセンター","消耗品費（freeeカード）"),
 ("セリア",10,"100均","消耗品費（freeeカード）"),
 ("ダイソー",10,"100均","消耗品費（freeeカード）"),
 ("ワークマン",10,"作業着","消耗品費（freeeカード）"),
 ("オフハウス",10,"備品","消耗品費（freeeカード）"),
 ("ドコモ",10,"通信","通信費"),
 ("USEN",10,"BGM","その他経費"),
 ("ダスキン",10,"清掃","その他経費"),
 ("LINE公式アカウント",10,"広告","広告宣伝費（共通宣伝費）"),
 ("ダゾーン",10,"放映","その他経費"),
 ("OPENAI",10,"ソフト","その他経費"),
 ("ﾗｸﾃﾝﾄﾗﾍﾞﾙ",10,"宿泊","旅費・交通費"),
 ("すき家",10,"外食","接待交際費"),
 ("大阪王将",10,"外食","接待交際費"),
 ("ｱﾎﾟﾛｽﾃ-ｼﾖﾝ",10,"燃料","旅費・交通費"),
 ("ｴﾈｵｽ",10,"燃料","旅費・交通費"),
 ("コスモ石油",10,"燃料","旅費・交通費"),
 ("カ)ﾐﾔｺ",10,"資材","仕入（その他）"),
 ("戸田産業",10,"資材","仕入（その他）"),
 ("ミズノ",10,"資材","仕入（その他）"),
 ("CHIMNEY TOWN",10,"物販","その他経費"),
 ("古河ＳＳ",10,"燃料","旅費・交通費"),   # 利用者確認済 2026-08-17

 # --- ローマ字表記のゆれ（2026-09-01 追加）--------------------------------
 # statement-2026-09.csv（8月利用分）から、カード会社が店名をローマ字で
 # 送ってくるようになった。ここに足したのは【上のマスタに既に居る同じ店】の
 # ローマ字綴りだけ。新しい取引先は足していない（推測しない約束のため）。
 ("TAIYO",8,"スーパー","仕入（freeeカード）"),            # タイヨー
 ("KASUMI",8,"スーパー","仕入（freeeカード）"),           # カスミ
 ("BIGHOUSE",8,"スーパー","仕入（freeeカード）"),         # ビッグハウス
 ("GIYOUMUSU-PA-",8,"スーパー","仕入（freeeカード）"),    # 業務スーパー
 ("KUSURINOAOKI",8,"ドラッグ","消耗品費（freeeカード）"),  # クスリのアオキ
 ("ミチノエキ",8,"直売所","仕入（freeeカード）"),           # 道の駅（半角カナ表記）
 ("SAKAGURAYAMANAKA",10,"酒","仕入（やまなか）"),         # 酒蔵やまなか
 # ★2025年の明細（statement-2025-10 / -12）は店名が半角カタカナで来る。
 #   norm() が NFKC で全角カタカナにそろえるので、下の綴りで拾える。
 #   2026年の明細は「酒蔵やまなか」「リカー＆フーズひしや」「戸田産業潮来営業所」
 #   と漢字まじりで来ていて、同じ店。2026-09-20 追加。
 ("サカグラヤマナカ",10,"酒","仕入（やまなか）"),           # ｻｶｸﾞﾗﾔﾏﾅｶ
 ("ヒシヤ",10,"酒","仕入（freeeカード）"),                # ﾘｶ-ｱﾝﾄﾞﾌ-ｽﾞﾋｼﾔ
 ("トダサンギヨウ",10,"資材","仕入（その他）"),             # ﾄﾀﾞｻﾝｷﾞﾖｳｲﾀｺ
 ("BESTLIQUR",10,"酒","仕入（freeeカード）"),             # タイヨーベストリカー
 ("MICREED",10,"業務用","仕入（freeeカード）"),           # ミクリード
 ("RAKSUL",10,"印刷","広告宣伝費（共通宣伝費）"),          # ラクスル
 ("RAKUTENTRAVEL",10,"宿泊","旅費・交通費"),             # 楽天トラベル

 # --- 新規取引先（利用者確認 2026-09-01）--------------------------------
 # statement-2026-09.csv（8月利用分）の保留リストを見てもらって決めたもの。
 ("明利酒類",10,"酒","仕入（freeeカード）"),
 #   ★明細のメモは「食材」だったが、利用者確認で【酒】。メモより指示が優先。
 ("ベイシア",8,"スーパー","仕入（freeeカード）"),           # 「食材仕入」
 ("山新グランステージ",10,"ホームセンター","消耗品費（freeeカード）"),
 ("セカンドストリ",10,"備品","消耗品費（freeeカード）"),      # ｾｶﾝﾄﾞｽﾄﾘ-ﾄｶﾐｽﾃﾝ
 ("HIGASHINIHONKOUSOKUDOURO",10,"高速道路","旅費・交通費"),  # 東日本高速道路
 ("JR EAST SHOPPING CENTER",10,"酒","仕入（freeeカード）"),
 ("ファーマーズマーケットなだろう",8,"直売所","仕入（freeeカード）"),
 # ★2026-09-20 追加。21期の12か月ぶんを読んだら、同じ店が別の綴りで出てきた。
 #   どれも上に既にある店のローマ字表記・全角表記。新しい取引先ではない。
 ("MEGADONQUIJOTE",8,"ディスカウント","仕入（freeeカード）"),   # MEGAドン・キホーテ
 ("SEIMIYA",8,"スーパー","仕入（freeeカード）"),                # セイミヤ
 ("PRINTPAC",10,"印刷","広告宣伝費（共通宣伝費）"),             # プリントパック
 ("DAZN",10,"放映","その他経費"),                            # ダゾーン（ＷＷＷ．ＤＡＺＮ．ＣＯＭ）
 ("DAISO",10,"100均","消耗品費（freeeカード）"),              # ダイソー
 ("SERIA",10,"100均","消耗品費（freeeカード）"),              # セリア
 ("MICHINOEKI",8,"直売所","仕入（freeeカード）"),               # 道の駅

 # ===== 2026-09-20 追加。21期12か月ぶんを読んで出てきた取引先 =====
 #   利用者に一覧を見せて費目を決めてもらった（「これでOK」＋個別の指示）。
 #   ★キーワードは norm()（NFKC＋空白除去＋大文字化）をかけて突き合わせるので、
 #     半角カタカナは全角で、全角英字は半角で書く。
 # --- 家電・ホームセンター・生活用品・資材 → 消耗品費 -----------------
 ("ケーズデンキ",10,"家電","消耗品費（freeeカード）"),
 ("KE-ZUDENKI",10,"家電","消耗品費（freeeカード）"),
 ("ヤマダ電機",10,"家電","消耗品費（freeeカード）"),
 ("ジョイフル本田",10,"ホームセンター","消耗品費（freeeカード）"),
 ("コーナン",10,"ホームセンター","消耗品費（freeeカード）"),
 ("WonderREX",10,"備品","消耗品費（freeeカード）"),
 ("ラクピ",10,"備品","消耗品費（freeeカード）"),
 ("テンポス",10,"厨房機器","消耗品費（freeeカード）"),
 ("電材部品",10,"資材","消耗品費（freeeカード）"),
 ("ジヤスポ",10,"資材","消耗品費（freeeカード）"),   # ｼﾞﾔｽﾎﾟ-ﾄ ｶﾐｻﾄ（ｰ が半角ハイフンになる）
 # 利用者 2026-09-20「SP LITIME-JP バッテリーです」
 ("SP LITIME",10,"バッテリー","消耗品費（freeeカード）"),
 # 利用者 2026-09-20「E STORE 消耗品」「ヤフー 消耗品」
 ("E STORE",10,"通販","消耗品費（freeeカード）"),
 ("ヤフー",10,"通販","消耗品費（freeeカード）"),
 # --- 酒・食材の仕入 → 仕入（freeeカード）------------------------------
 #   ★領収書メモに「酒」「食材」があるものは、そちらの税率が優先される。
 #     下の税率はメモが無いときの控え。
 ("泉屋",10,"酒","仕入（freeeカード）"),
 ("ライフクシユゾウ",10,"酒","仕入（freeeカード）"),          # 来福酒造 大吉蔵
 ("馬場本店酒造",10,"酒","仕入（freeeカード）"),
 ("山田屋酒店",10,"酒","仕入（freeeカード）"),
 ("松蔵屋",10,"酒","仕入（freeeカード）"),
 ("酒の松本",10,"酒","仕入（freeeカード）"),
 ("サケノマツモト",10,"酒","仕入（freeeカード）"),
 # 利用者 2026-09-20「藤原ストアは仕入れ」。もも焼きJAPANには
 # 「仕入（藤原ストア）」の専用行が無いので 仕入（freeeカード）へ
 ("藤原ストア",10,"酒","仕入（freeeカード）"),
 ("飯島畜産",8,"食材","仕入（freeeカード）"),
 ("肉の丸七",8,"食材","仕入（freeeカード）"),
 ("さかな工房",8,"食材","仕入（freeeカード）"),
 ("元気豚",8,"食材","仕入（freeeカード）"),
 ("ハツコウノサト",8,"直売所","仕入（freeeカード）"),         # 発酵の里こうざき新鮮市場
 ("オオタカエン",8,"食材","仕入（freeeカード）"),
 ("CURINOMI",8,"食材","仕入（freeeカード）"),
 ("STYLEKITCHEN",8,"食材","仕入（freeeカード）"),
 ("エクセル　ミナミ",8,"食材","仕入（freeeカード）"),
 # ★この3件は領収書メモが「食材」だった。店の業種ではなく買ったものに合わせる
 ("無印良品",8,"食材","仕入（freeeカード）"),
 ("セレクト館",8,"食材","仕入（freeeカード）"),
 ("ヤックスドラッグ",8,"食材","仕入（freeeカード）"),
 # 利用者 2026-09-20「ソフトバンクセレクション 仕入れ」「WINDSBASE 仕入れ」
 ("ソフトバンクセレクション",10,"仕入","仕入（freeeカード）"),
 ("WINDSBASE",8,"食材","仕入（freeeカード）"),
 ("YAKITATEYAKASHIMATEN",10,"自店","仕入（freeeカード）"),   # ±750で相殺。差引0
 # --- 外食 → 接待交際費（税抜5,000円以下は自動で会議費へ）--------------
 ("山内農場",10,"外食","接待交際費"),
 ("松のや",10,"外食","接待交際費"),
 ("まい泉",10,"外食","接待交際費"),
 ("吉野家",10,"外食","接待交際費"),
 ("めん王",10,"外食","接待交際費"),
 ("餃子の王将",10,"外食","接待交際費"),
 # --- 宿泊・駐車場 → 旅費・交通費 --------------------------------------
 ("たびのホテル",10,"宿泊","旅費・交通費"),
 ("リパーク",10,"駐車場","旅費・交通費"),
 ("ナビパーク",10,"駐車場","旅費・交通費"),
 # --- 印刷 → 広告宣伝費 -------------------------------------------------
 ("キングプリンタ",10,"印刷","広告宣伝費（共通宣伝費）"),
 # --- その他 -----------------------------------------------------------
 # 利用者 2026-09-20「Canva 年会費」。神栖横丁に一般の「諸会費」行が無いので
 # いまは その他経費。専用行がほしければ言ってください
 ("CANVA",10,"ソフト年会費","その他経費"),
 # 利用者 2026-09-20「日本郵便 切手」。郵便切手は非課税なので税率0
 ("日本郵便",0,"切手","通信費（郵便関係・その他）"),
 #   ★この2件も利用者確認 2026-09-01「Okです」。明細のメモ（酒／食材）どおりで正しかった。
]

# ---- カード名(下4桁) → 店舗タブ ----
CARD2TAB = {
 "4188":"タコとハイボール", "0538":"さわら十三里屋", "9782":"さわら十三里屋",
 "7655":"韓国酒場ハナ", "0207":"もも焼きJAPAN", "2739":"りゅうちゃん",
 "0585":"焼きたて屋", "8772":"神栖横丁", "3870":"神栖横丁",
 "0321":"業務課", "0819":"鳥害対策課",
}
# 除外: 十三里屋 固定費カード0538 の ドコモ/USEN/ダスキン（請求書側で計上）
EXCLUDE_0538 = ("ドコモ","USEN","ダスキン")
# メモ判定
MEMO10 = ("酒","資材","炭")
MEMO8  = ("食材",)

# 税率が矛盾したときに勝つキーワード（利用者確認済 2026-08-17）
# 例: 「セリア　カスミ神栖店」→セリア10% / 「タイヨーベストリカー神栖店」→ベストリカー10%
OVERRIDE_WINNERS = ("セリア", "ベストリカー", "BESTLIQUR")
# ★BESTLIQUR は「TAIYOBESTLIQUR KAMISU」が TAIYO(8%) と両方に当たるため。
#   ベストリカーは酒屋なので10%が正しい（日本語表記のときと同じ扱い）。


def norm(s):
    """全角/半角・大小文字を揃えて比較用に正規化"""
    if s is None or (isinstance(s, float) and math.isnan(s)):
        return ""
    s = unicodedata.normalize("NFKC", str(s))
    return s.replace("　", " ").replace(" ", "").upper()


def merchant_only(riyouten, cardname):
    """利用店名は「<店名> <カード名>」形式。末尾のカード名を除いて店名部分だけ返す。
    カード名にマスタ語(コメリ/横丁など)が含まれ誤ヒットするのを防ぐ。"""
    s = str(riyouten)
    c = str(cardname)
    if c and s.endswith(c):
        return s[: -len(c)].strip()
    # 正規化しても末尾一致するか確認（全角スペース等の揺れ対策）
    ns, nc = norm(s), norm(c)
    if nc and ns.endswith(nc):
        # 元文字列側で対応する長さを削る
        cut = len(s)
        while cut > 0 and norm(s[:cut]).endswith(nc):
            cut -= 1
        return s[: cut + 1].strip()
    return s.strip()


def match_master(name):
    """マスタ一致（複数ヒットは全部返す）"""
    n = norm(name)
    hits = [m for m in MASTER if norm(m[0]) and norm(m[0]) in n]
    if not hits:
        return []
    # 最長キーワード優先で並べる
    hits.sort(key=lambda m: len(norm(m[0])), reverse=True)
    return hits


def memo_rate(memo):
    n = norm(memo)
    if not n:
        return None, None
    if any(norm(k) in n for k in MEMO10):
        return 10, "メモ"
    if any(norm(k) in n for k in MEMO8):
        return 8, "メモ"
    return None, None


# 21期 ＝ 2025年9月 〜 2026年8月。PL列はこの範囲だけ。
PERIOD = [(2025, m) for m in range(9, 13)] + [(2026, m) for m in range(1, 9)]


def pl_col(year, month):
    """(年, 月) → PL列。21期の外なら None（＝この期には入れない）。"""
    return f"{month}月" if (year, month) in PERIOD else None


def load(path, riyou_year, riyou_month):
    """★年も持たせる（2026-09-20）。

    もとは月の数字だけだった。酒蔵やまなかの「1〜5日は前月へ」が年をまたぐと
    2025年9月1〜5日 → 「8月」になり、21期の最終月＝2026年8月の列に入ってしまう
    （2025年8月は20期）。年を持たせて pl_col() で外に出す。
    """
    d = pd.read_csv(path, encoding="utf-8-sig", dtype=str)
    d = d[d["利用日"].notna() & (d["利用日"].astype(str).str.strip() != "")]
    d["利用額"] = d["利用額"].astype(int)
    d["利用日"] = pd.to_datetime(d["利用日"])
    d["_riyou_year"] = riyou_year
    d["_riyou_month"] = riyou_month
    return d


def classify(d):
    rows, hold = [], []
    for _, r in d.iterrows():
        card4 = str(r["カード下4桁"]).zfill(4)
        tab = CARD2TAB.get(card4)
        name_raw = r["利用店名"]
        name = merchant_only(name_raw, r["カード名"])
        memo = r.get("領収書メモ")
        amt = int(r["利用額"])
        day = r["利用日"]
        base = dict(利用日=day.strftime("%Y-%m-%d"), 店舗=tab, 取引先=name,
                    摘要=("" if pd.isna(memo) else memo), 税込=amt,
                    カード=f'{r["カード名"]}({card4})', 元ファイル=r["_srcfile"])

        if tab is None:
            hold.append({**base, 理由: "カード下4桁が未定義"} if False else
                        {**base, "理由": f"カード下4桁 {card4} がタブ対応表にない"})
            continue

        # 除外ルール
        if card4 == "0538" and any(norm(k) in norm(name) for k in EXCLUDE_0538):
            base.update(判定="除外", 税率="", 税抜="", 消費税="", PL行="（除外）二重計上防止",
                        計上月="", 備考="十三里屋固定費カード0538: 請求書側で計上")
            rows.append(base); continue

        hits = match_master(name)
        rate, src = memo_rate(memo)

        if rate is None:                      # 優先1で決まらない → 優先2
            if not hits:
                hold.append({**base, "理由": "マスタにキーワード一致なし・メモもなし"})
                continue
            distinct = {h[1] for h in hits}
            if len(distinct) > 1:
                win = [h for h in hits if h[0] in OVERRIDE_WINNERS]
                if len(win) == 1:
                    hits = win + [h for h in hits if h not in win]
                    src_note = "マスタ(優先ルール)"
                else:
                    hold.append({**base, "理由": "マスタが複数ヒットし税率が矛盾: " +
                                 " / ".join(f"{h[0]}={h[1]}%" for h in hits)})
                    continue
            else:
                src_note = "マスタ"
            rate, src = hits[0][1], src_note

        if not hits:
            hold.append({**base, "理由": f"メモから税率{rate}%は判るが、PL行を決めるマスタ一致がない"})
            continue

        kw, mrate, cat, plrow = hits[0]

        # 酒蔵やまなか 特別ルール: 利用日1〜5日は前月分へ
        # ★年つきで持つ（2026-09-20）。月の数字だけだと年をまたいだときに
        #   別の期の列へ入ってしまう。
        # ★計上月は【利用日】から決める（従来どおり）。ファイル名の年月は
        #   目安でしかなく、月末・月初が隣のファイルに入ることがあるため。
        #   6〜8月は既存PLとの突き合わせでこの決め方を検証済み。
        year, month = day.year, day.month
        biko = ""
        if "やまなか" in norm(kw) or "ヤマナカ" in norm(name):
            if 1 <= day.day <= 5:
                prev = day.replace(day=1) - pd.Timedelta(days=1)
                year, month = prev.year, prev.month
                biko = "前月分に振替"
        col = pl_col(year, month)
        if col is None:
            # 21期の外（2025年8月以前）。利用者判断 2026-09-20
            #   「2025.8月はPLがないから、それは無視してもらっていい」
            hold.append({**base, "理由":
                         f"振替先が {year}年{month}月 で21期（2025年9月〜2026年8月）の外。"
                         "利用者判断 2026-09-20『2025.8月はPLがないから無視でいい』"})
            continue

        # 円未満切り捨て。floatだと 30800/1.1=27999.99… で1円ずれるため Decimal を使う
        tax_ex = int((Decimal(amt) / (1 + Decimal(rate) / 100)).to_integral_value(ROUND_FLOOR))
        # ★1件5,000円以下（税抜）の接待交際費は会議費へ（利用者指示 2026-08-22）。
        #   cards.py（JCB/三井住友）と同じ扱い。判定はこの損益計算書に合わせて税抜。
        if plrow == "接待交際費" and tax_ex <= 5000:
            plrow = "会議費"
            biko = (biko + "／" if biko else "") + "1件5,000円以下なので会議費"
        base.update(判定=src, 税率=rate, 税抜=tax_ex, 消費税=amt - tax_ex,
                    PL行=plrow, 計上月=col, 備考=biko, 一致キーワード=kw, 分類=cat)
        rows.append(base)
    return pd.DataFrame(rows), pd.DataFrame(hold)


if __name__ == "__main__":
    frames = []
    # ★ファイル名の年月 −1 が PL列。statement-2026-09 は【8月利用分】
    # ★21期の12か月ぶん（利用者指示 2026-09-20「全月をカード明細から入れ直してみて」）。
    #   ファイル名の年月 −1 が利用月＝PL列。statement-2026-09 は【2026年8月利用分】。
    for path, y, m in [("csv/statement-2025-10.csv", 2025,  9),
                       ("csv/statement-2025-11.csv", 2025, 10),
                       ("csv/statement-2025-12.csv", 2025, 11),
                       ("csv/statement-2026-01.csv", 2025, 12),
                       ("csv/statement-2026-02.csv", 2026,  1),
                       ("csv/statement-2026-03.csv", 2026,  2),
                       ("csv/statement-2026-04.csv", 2026,  3),
                       ("csv/statement-2026-05.csv", 2026,  4),
                       ("csv/statement-2026-06.csv", 2026,  5),
                       ("csv/statement-2026-07.csv", 2026,  6),
                       ("csv/statement-2026-08.csv", 2026,  7),
                       ("csv/statement-2026-09.csv", 2026,  8)]:
        src = os.path.basename(path)
        d = load(path, y, m); d["_srcfile"] = src; frames.append(d)
    d = pd.concat(frames, ignore_index=True)
    ok, hold = classify(d)
    ok.to_csv("out_meisai.csv", index=False, encoding="utf-8-sig")
    hold.to_csv("out_hold.csv", index=False, encoding="utf-8-sig")
    print("判定OK", len(ok), "／ 保留", len(hold))
